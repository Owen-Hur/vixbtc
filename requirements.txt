# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet I/O
scipy>=1.11
python-dateutil>=2.8
pytz>=2023.3

# Modeling
torch>=2.0             # LSTM Autoencoder
scikit-learn>=1.3      # StandardScaler, PCA
xgboost>=2.0           # direction classifier (rejected experiment)
statsmodels>=0.14,<0.15  # Granger causality, OLS
                       #   ↑ 0.15+ 는 grangercausalitytests(verbose=) 인자를 제거했습니다.
                       #     최신 버전을 쓰려면 analysis/vix_overnight_granger/code/vix_granger.py 의
                       #     verbose=False 인자를 삭제하십시오 (결과는 동일하게 재현됩니다).
joblib>=1.3

# Data collection
yfinance>=0.2.40       # VIX / VIXY / global markets
ccxt>=4.2              # Binance funding rate
requests>=2.31
aiohttp>=3.9           # async downloads
websockets>=12.0       # realtime BTC collector

# Visualization
matplotlib>=3.7
plotly>=5.18

# Utils
tqdm>=4.66

# Optional — VIXY 1m via Alpaca Market Data API (data_collection/fetch_vixy_1m.py)
# alpaca-py>=0.20
